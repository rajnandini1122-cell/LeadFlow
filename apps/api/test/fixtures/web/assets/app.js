// A stand-in for a fingerprinted Vite asset, used only by the E2E suite.
// Its contents are irrelevant; what is asserted is that a REAL file is served
// as itself rather than being rewritten to the SPA shell.
export const marker = 'leadflow-spa-asset';
